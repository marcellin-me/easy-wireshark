{
  "name": "packet-scope-cli",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Interactive macOS CLI for selecting a network interface, choosing a process, and viewing its packets by OSI layer.",
  "scripts": {
    "capture": "node src/index.js",
    "check": "node --check src/index.js"
  },
  "engines": {
    "node": ">=20"
  }
}
